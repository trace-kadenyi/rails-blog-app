<section>
  <div class='one_user'>
    <div class='user_photo'>
      <img src='<%= @user.photo %>' alt='<%= @user.name %> thumbnail'/>
    </div>
    <div class='user_info'>
      <h2><%= @user.name %></h2>
      <p>Number of posts: <%= @user.posts.count %></p>
    </div>
  </div>
  <h5><%= link_to 'Add new post', new_post_path %></h5>
  <div class='user_posts'>
    <% @user.posts.each do |post| %>
      <div class='posts_list'>
        <h5><%= link_to post.title, user_post_path(@user, post) %></h5>
        <p><%= post.text %></p>
        <span class='like_comment'>
          Comments: <%= post.comments.count %>,
          Likes: <%= post.likes.count %>
        </span>
        <h5><%= render partial: 'likes/form', locals: {post: post} %></h5>
      </div>
      <ul class='post_comment'>
        <% if post.return_recent_comments.length == 0 %>
          <p>No comment added!</p>
        <% else %>
          <% post.return_recent_comments.each do |comment| %>
            <li>
              <%= comment.users.name %>: <%= comment.text %>
            </li>
          <% end %>
        <% end %>
        <h5><%= render partial: 'comments/form', locals: {post: post} %></h5>
      </ul>
    <% end %>
  </div>
</section>